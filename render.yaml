services:
  - type: web
    name: yt-transcript-mcp
    runtime: docker
    plan: free
    env:
      - key: NODE_ENV
        value: production
      - key: TRANSCRIPT_METHOD
        value: youtubei
    healthCheckPath: /health
